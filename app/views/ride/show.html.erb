Your Uber is on its way! You will save at least <%= number_to_currency(params[:saved_cost], :unit => "$") %> with our sharing Uber sharing system.<br>

<%= link_to "I'm home!", {controller: "ride", action: "dropoff", driver_id: @driver.id} %>